\section{Design}
\label{design}
%Architecture:
    %system (description, network)
    %clients
        %description (function, local storage)
        %actions
            %discovery
            %initialization
            %local update
            %broker ACK
            %broker PUSHBACK
            %broker PULL
    %brokers
        %description (function, local storage)
        %actions
            %connect
            %client RQST
            %[distr] broker PUSH
            %[distr] broker PULL
    %messages
